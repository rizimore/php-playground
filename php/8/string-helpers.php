<?php

$s = "sp19-bse-006";

// string start with
if (str_starts_with($s, "sp19")) {
    echo "string start with";
}

// string contains
if (str_contains($s, "bse")) {
    echo "string contains";
}

// string ends with
if (str_ends_with($s, "006")) {
    echo "string end with";
}
